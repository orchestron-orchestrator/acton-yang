Container({
  'nested': Container({
    'f:inner': Container({
      'li1': List(['name'], elements=[
        Container({
          'name': Leaf(BBB),
          'f:bar': Leaf(p2)
        }, ['BBB'])
      ])
    })
  })
})