Container({
  'nested': Container({
    'inner': Container({
      'li1': List(['name'], elements=[
        Container({
          'name': Leaf(BBB),
          'bar': Leaf(p2)
        }, ['BBB'])
      ])
    })
  })
})