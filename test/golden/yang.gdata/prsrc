Module('moo', children=[
  Container('foo', children=[
    Leaf('a', 1),
    List('l1', ['name'], elements=[
      ListElement(['k1'], children=[
        Leaf('n1', 1),
        Leaf('n2', 2)
      ]),
      ListElement(['k4'], children=[
        Leaf('n4', 4)
      ])
    ])
  ])
])