import base64
import json
import xml
import yang
import yang.adata
import yang.gdata
import yang.gen3
import yang.xpath
from yang.identityref import Identityref, PartialIdentityref
from yang.pattern import YangPattern
from yang.schema import *
from yang.type import Decimal, Ranges

# == This file is generated ==




_identities: list[DIdentity] = []


SRC_DNODE = DRoot(children=[
    DContainer(module='base', namespace='http://example.com/base', prefix='base', name='c1', config=True, presence=False, children=[
        DContainer(module='base', namespace='http://example.com/base', prefix='base', name='c2', config=True, presence=False, children=[
            DLeaf(module='base', namespace='http://example.com/base', prefix='base', name='foo', config=True, mandatory=False, type_=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))
        ]),
        DContainer(module='foo', namespace='http://example.com/foo', prefix='foo', name='c2', config=True, presence=False, children=[
            DLeaf(module='foo', namespace='http://example.com/foo', prefix='foo', name='foo', config=True, mandatory=False, type_=DTypeString(name='string', description=None, reference=None, exts=[], builtin_type='string', default=None, length=None, patterns=[]))
        ])
    ])
])

NS_base = 'http://example.com/base'
NS_foo = 'http://example.com/foo'


class base__c1__base_c2(yang.adata.MNode):
    foo: ?str

    mut def __init__(self, foo: ?str):
        self._ns = 'http://example.com/base'
        self.foo = foo

    def _get_attr(self, name: str) -> ?value:
        if name == 'foo':
            return self.foo
        raise ValueError('Attribute {name} not found in base__c1__base_c2')

    mut def to_gdata(self) -> yang.gdata.Node:
        if isinstance(self, yang.adata.MNode):
            return yang.gen3.from_data(SRC_DNODE, self, loose=False, root_path=['base:c1', 'c2'])
        raise ValueError('unreachable')

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> base__c1__base_c2:
        if n is not None:
            return base__c1__base_c2(foo=n.get_opt_str(yang.gdata.Id(NS_base, 'foo')))
        return base__c1__base_c2()

    def copy(self):
        """Create a deep copy of this adata object"""
        return base__c1__base_c2.from_gdata(self.to_gdata())


class base__c1__foo_c2(yang.adata.MNode):
    foo: ?str

    mut def __init__(self, foo: ?str):
        self._ns = 'http://example.com/foo'
        self.foo = foo

    def _get_attr(self, name: str) -> ?value:
        if name == 'foo':
            return self.foo
        raise ValueError('Attribute {name} not found in base__c1__foo_c2')

    mut def to_gdata(self) -> yang.gdata.Node:
        if isinstance(self, yang.adata.MNode):
            return yang.gen3.from_data(SRC_DNODE, self, loose=False, root_path=['base:c1', 'foo:c2'])
        raise ValueError('unreachable')

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> base__c1__foo_c2:
        if n is not None:
            return base__c1__foo_c2(foo=n.get_opt_str(yang.gdata.Id(NS_foo, 'foo')))
        return base__c1__foo_c2()

    def copy(self):
        """Create a deep copy of this adata object"""
        return base__c1__foo_c2.from_gdata(self.to_gdata())


class base__c1(yang.adata.MNode):
    base_c2: base__c1__base_c2
    foo_c2: base__c1__foo_c2

    mut def __init__(self, base_c2: ?base__c1__base_c2=None, foo_c2: ?base__c1__foo_c2=None):
        self._ns = 'http://example.com/base'
        self.base_c2 = base_c2 if base_c2 is not None else base__c1__base_c2()
        self.foo_c2 = foo_c2 if foo_c2 is not None else base__c1__foo_c2()

    def _get_attr(self, name: str) -> ?value:
        if name == 'base_c2':
            return self.base_c2
        if name == 'foo_c2':
            return self.foo_c2
        raise ValueError('Attribute {name} not found in base__c1')

    mut def to_gdata(self) -> yang.gdata.Node:
        if isinstance(self, yang.adata.MNode):
            return yang.gen3.from_data(SRC_DNODE, self, loose=False, root_path=['base:c1'])
        raise ValueError('unreachable')

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> base__c1:
        if n is not None:
            return base__c1(base_c2=base__c1__base_c2.from_gdata(n.get_opt_cnt(yang.gdata.Id(NS_base, 'c2'))), foo_c2=base__c1__foo_c2.from_gdata(n.get_opt_cnt(yang.gdata.Id(NS_foo, 'c2'))))
        return base__c1()

    def copy(self):
        """Create a deep copy of this adata object"""
        return base__c1.from_gdata(self.to_gdata())


class root(yang.adata.MNode):
    c1: base__c1

    mut def __init__(self, c1: ?base__c1=None):
        self._ns = ''
        self.c1 = c1 if c1 is not None else base__c1()

    def _get_attr(self, name: str) -> ?value:
        if name == 'c1':
            return self.c1
        raise ValueError('Attribute {name} not found in root')

    mut def to_gdata(self) -> yang.gdata.Node:
        if isinstance(self, yang.adata.MNode):
            return yang.gen3.from_data(SRC_DNODE, self, loose=False)
        raise ValueError('unreachable')

    @staticmethod
    mut def from_gdata(n: ?yang.gdata.Node) -> root:
        if n is not None:
            return root(c1=base__c1.from_gdata(n.get_opt_cnt(yang.gdata.Id(NS_base, 'c1'))))
        return root()

    def copy(self):
        """Create a deep copy of this adata object"""
        return root.from_gdata(self.to_gdata())


schema_namespaces: set[str] = {
    'http://example.com/base',
    'http://example.com/foo',
}
